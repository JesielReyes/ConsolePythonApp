[project]
name = "consolepythonapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "DarrenM-Lab", email = "medy.darren@gmai.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
consolepythonapp = "consolepythonapp:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
