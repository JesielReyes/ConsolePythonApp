ToDo's
1. accounts
2, User stuff
3. Transactions



Unique Selling Point: Coin Flip + Category Tracker

Roadmap

–  Rest API Backend / CRUD/ MVC/ Controller, Services, Repo,
Design Patterns (Factory, Singleton)

-TDD: Add testing (assertions, service layer, controllers, repo, unit-tests, mockito)

-API Testing using Postman
-API Documentation using Swagger

- Databases (Relational: Postgres, Non-Relational or Collection-Bases: MongoDB Cloud Atlas)

Database Integration: ORM

-React and React Frontend

-Devops: Git, Github, CI-CD , GitHub Actions

-Security Considerations: password hashing/salting , BCrypt, Authentication, Authorization, JWT

-Cloud Concepts/ AWS/ AWS Deployment

-IAC: Terraform
—---------------------------------------------------------------------------------------------------------------------------------

Bank App Usecase: Single Flow Console App

– Single Flow: You should run it only once, and see the whole flow

- Main/Runner class -> main() -> call everything from within main

-Classes: 
Bank Class
Abstract User Class 
Child classes: “Admin”, “Customer” extending from User

Abstract Account Class
Child Classes: “SavingsAccount”, “CheckingsAccount” extending from Account

“AccountOperations” as an interface: withdraw(), deposit()

SavingsAccount extends Account implements AccountOperations{
  Withdraw () {
      // does not allow overdraft
   } 
}

CheckingsAccount extends Account implements AccountOperations{
  Withdraw () {
      // allow overdraft
   } 
}

– Runner -> printMessage( String message) {

}
-> Registered Seed Data for users, some accounts
-> Login ( Scanner, Console.ReadLine…..username/password) -> check against registered data -> deny/allow access

-> Check if admin or customer :-> AdminDashboard() / CustomerDashboard()

-> AdminDashboard: admin options: ViewAllCustomers, ViewAllAccounts, ………CRUD

-> CustomerDashboard:customer options: view his/her own account/accounts, deposit, withdraw, transfer

-> Concluding Message


Variables, loops, conditionals, ternary, switch-case, OOD( classes, objects, constructors, overloading constructors, attributes with getters/setters ENCAPSULATION, methods, method overloading, inheritance , method overriding
Strings, collections, Arrays, List, Map/ Dictionary, Sets, TreeSet…………..
